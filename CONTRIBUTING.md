# 🤝 راهنمای مشارکت در WonderWay

خوشحالیم که میخواهید در پروژه WonderWay مشارکت کنید! این راهنما به شما کمک میکند تا بهترین نحوه مشارکت را بیاموزید.

## 📋 فهرست مطالب

- [کد رفتار](#-کد-رفتار)
- [نحوه مشارکت](#-نحوه-مشارکت)
- [استانداردهای کد](#-استانداردهای-کد)
- [راهاندازی محیط توسعه](#-راهاندازی-محیط-توسعه)
- [نوشتن تست](#-نوشتن-تست)
- [گزارش باگ](#-گزارش-باگ)
- [پیشنهاد ویژگی](#-پیشنهاد-ویژگی)

## 📜 کد رفتار

### تعهدات ما
- احترام به همه مشارکتکنندگان
- ایجاد محیطی امن و دوستانه
- پذیرش انتقادات سازنده
- تمرکز روی بهترین راهحل برای جامعه

### رفتارهای قابل قبول
- ✅ استفاده از زبان دوستانه و شامل همه
- ✅ احترام به نظرات و تجربیات مختلف
- ✅ پذیرش انتقادات سازنده
- ✅ تمرکز روی بهترین راهحل برای پروژه

### رفتارهای غیرقابل قبول
- ❌ استفاده از زبان یا تصاویر جنسی
- ❌ حملات شخصی یا سیاسی
- ❌ آزار عمومی یا خصوصی
- ❌ انتشار اطلاعات خصوصی دیگران

## 🚀 نحوه مشارکت

### 1. Fork کردن پروژه
```bash
# Fork کردن repository در GitHub
# سپس clone کردن fork شما
git clone https://github.com/YOUR-USERNAME/wonderway-backend.git
cd wonderway-backend
```

### 2. راهاندازی محیط توسعه
```bash
# نصب dependencies
composer install
npm install

# کپی فایل محیط
cp .env.example .env

# تولید کلید
php artisan key:generate

# راهاندازی دیتابیس
php artisan migrate --seed
```

### 3. ایجاد Branch جدید
```bash
# برای ویژگی جدید
git checkout -b feature/amazing-feature

# برای رفع باگ
git checkout -b bugfix/fix-issue-123

# برای بهبود عملکرد
git checkout -b performance/optimize-queries
```

### 4. انجام تغییرات
- کد خود را بنویسید
- تستهای مربوطه را اضافه کنید
- مستندات را بروزرسانی کنید

### 5. Commit کردن
```bash
# استفاده از Conventional Commits
git commit -m "feat: add user profile editing feature"
git commit -m "fix: resolve timeline loading issue"
git commit -m "docs: update API documentation"
```

### 6. Push و Pull Request
```bash
git push origin feature/amazing-feature
```
سپس در GitHub یک Pull Request ایجاد کنید.

## 📏 استانداردهای کد

### PHP Coding Standards

#### PSR-12 Compliance
```php
<?php

namespace App\Services;

use App\Models\User;
use Illuminate\Support\Facades\Hash;

class UserService
{
    public function createUser(array $data): User
    {
        return User::create([
            'name' => $data['name'],
            'email' => $data['email'],
            'password' => Hash::make($data['password']),
        ]);
    }
}
```

#### نامگذاری
- **Classes**: PascalCase (`UserService`)
- **Methods**: camelCase (`createUser`)
- **Variables**: camelCase (`$userData`)
- **Constants**: UPPER_SNAKE_CASE (`MAX_UPLOAD_SIZE`)

#### DocBlocks
```php
/**
 * Create a new user account
 *
 * @param array $data User registration data
 * @return User Created user instance
 * @throws ValidationException When data is invalid
 */
public function createUser(array $data): User
{
    // Implementation
}
```

### بررسی کیفیت کد
```bash
# بررسی استانداردهای کد
composer run-script cs-check

# اصلاح خودکار
composer run-script cs-fix

# تحلیل استاتیک (اختیاری)
composer run-script analyse
```

## 🧪 نوشتن تست

### ساختار تستها
```
tests/
├── Feature/          # تستهای کامل ویژگیها
├── Unit/            # تستهای واحد
└── TestCase.php     # کلاس پایه تستها
```

### نمونه تست Feature
```php
<?php

namespace Tests\Feature;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class PostTest extends TestCase
{
    use RefreshDatabase;

    #[\PHPUnit\Framework\Attributes\Test]
    public function user_can_create_post(): void
    {
        $user = User::factory()->create();

        $response = $this->actingAs($user)
            ->postJson('/api/posts', [
                'content' => 'Test post content',
            ]);

        $response->assertStatus(201)
            ->assertJsonStructure(['id', 'content', 'user']);
    }
}
```

### نمونه تست Unit
```php
<?php

namespace Tests\Unit;

use App\Services\PostService;
use Tests\TestCase;

class PostServiceTest extends TestCase
{
    #[\PHPUnit\Framework\Attributes\Test]
    public function can_sanitize_content(): void
    {
        $service = new PostService();
        
        $dirty = '<script>alert("xss")</script>Hello';
        $clean = $service->sanitizeContent($dirty);
        
        $this->assertEquals('Hello', $clean);
    }
}
```

### اجرای تستها
```bash
# اجرای تمام تستها
php artisan test

# اجرای تست خاص
php artisan test --filter=PostTest

# اجرای تستها با coverage
php artisan test --coverage
```

## 🐛 گزارش باگ

### قبل از گزارش باگ
1. جستجو کنید که آیا باگ قبلاً گزارش شده
2. آخرین نسخه را امتحان کنید
3. مراحل بازتولید باگ را مشخص کنید

### الگوی گزارش باگ
```markdown
## توضیح باگ
توضیح مختصر و واضح از باگ

## مراحل بازتولید
1. برو به '...'
2. کلیک روی '...'
3. اسکرول کن تا '...'
4. خطا را ببین

## رفتار مورد انتظار
توضیح اینکه چه اتفاقی باید میافتاد

## رفتار واقعی
توضیح اینکه چه اتفاقی افتاده

## اسکرینشات
در صورت امکان اسکرینشات اضافه کنید

## محیط
- OS: [e.g. Windows 11]
- PHP Version: [e.g. 8.2.0]
- Laravel Version: [e.g. 12.0]
- Browser: [e.g. Chrome 120]

## اطلاعات اضافی
هر اطلاعات اضافی که ممکن است مفید باشد
```

## 💡 پیشنهاد ویژگی

### الگوی پیشنهاد ویژگی
```markdown
## خلاصه ویژگی
توضیح مختصر از ویژگی پیشنهادی

## انگیزه
چرا این ویژگی مفید است؟ چه مشکلی را حل میکند؟

## راهحل پیشنهادی
توضیح دقیق از نحوه پیادهسازی

## جایگزینهای در نظر گرفته شده
راهحلهای دیگری که در نظر گرفتهاید

## اطلاعات اضافی
هر اطلاعات اضافی، mockup، یا مثال
```

## 🔍 بررسی Pull Request

### چک لیست قبل از ارسال PR
- [ ] کد با استانداردهای پروژه مطابقت دارد
- [ ] تستهای مربوطه نوشته شده
- [ ] تمام تستها پاس میشوند
- [ ] مستندات بروزرسانی شده
- [ ] تغییرات breaking مستند شدهاند
- [ ] Commit messageها واضح هستند

### فرآیند بررسی
1. **Automated Checks**: CI/CD pipeline اجرا میشود
2. **Code Review**: حداقل یک maintainer بررسی میکند
3. **Testing**: تستهای اضافی در صورت نیاز
4. **Merge**: پس از تأیید merge میشود

## 🏷️ انواع مشارکت

### 🐛 Bug Fixes
- رفع مشکلات موجود
- بهبود پایداری
- رفع مسائل امنیتی

### ✨ Features
- ویژگیهای جدید
- بهبود ویژگیهای موجود
- API جدید

### 📚 Documentation
- بهبود README
- مستندات API
- راهنماهای کاربری

### 🎨 UI/UX
- بهبود رابط کاربری
- تجربه کاربری بهتر
- طراحی responsive

### ⚡ Performance
- بهینهسازی کوئریها
- کاهش زمان پاسخ
- بهبود مصرف حافظه

### 🧪 Testing
- افزایش coverage
- تستهای جدید
- بهبود کیفیت تستها

## 🎯 اولویتها

### High Priority
- مسائل امنیتی
- باگهای critical
- مسائل عملکرد

### Medium Priority
- ویژگیهای جدید
- بهبود UX
- بهینهسازیها

### Low Priority
- refactoring
- بهبود مستندات
- تستهای اضافی

## 📞 ارتباط با تیم

### راههای ارتباط
- **GitHub Issues**: برای باگها و ویژگیها
- **GitHub Discussions**: برای سوالات عمومی
- **Discord**: برای گفتگوی real-time
- **Email**: برای مسائل حساس

### زمان پاسخ
- **Issues**: 24-48 ساعت
- **Pull Requests**: 2-5 روز کاری
- **Security Issues**: 24 ساعت

## 🏆 تشکر از مشارکتکنندگان

همه مشارکتکنندگان در فایل [CONTRIBUTORS.md](CONTRIBUTORS.md) ذکر میشوند.

### انواع مشارکت قابل تشکر
- کد نویسی
- گزارش باگ
- پیشنهاد ویژگی
- بهبود مستندات
- تست
- طراحی
- ترجمه

---

**متشکریم که به بهبود WonderWay کمک میکنید! 🙏**